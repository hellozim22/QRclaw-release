# 2026-06-28 Dev Log

## Summary
Added a macOS desktop update-check path for QRClaw using Sparkle 2 and a WebView bridge entry in Personal Center.

## Changes
- [feat] Added Sparkle 2 to the macOS SwiftPM app and introduced `UpdateService` for version metadata and native update checks.
- [feat] Extended the WKWebView desktop bridge with `getAppVersion` and `checkForUpdates`.
- [feat] Added a "产品更新" card to `个人中心` with current version and a "检测更新" button.
- [feat] Extended desktop packaging to copy/sign `Sparkle.framework`, write optional `SUFeedURL` / `SUPublicEDKey` Info.plist keys, and optionally generate Sparkle appcasts for release archives.
- [ops] Configured local Sparkle update settings for GitHub Releases repo `hellozim22/QRclaw_release` and generated the Sparkle public key. Private key remains in local Keychain.
- [ops] Added `scripts/build-github-update-release.sh` and `docs/desktop-updates.md` for repeatable GitHub Releases update packaging.
- [test] Added desktop bridge and settings-page tests, and extended desktop static verification for update-check wiring.

## Test Results
| Metric | Value |
|--------|-------|
| Web update tests | pass |
| Web build | pass |
| macOS Swift build | pass |
| Desktop static verify | pass |
| Desktop launch verify | pass |
| DMG build | pass (`dist/QRClaw.dmg`) |
| GitHub update artifact build | pass (`dist/github-update-release/v0.1.1`) |

## Decisions
- Use Sparkle 2 for macOS updates instead of letting the web frontend download or replace the app bundle.
- Manual "检测更新" is available now. Background polling starts only when a real `QRCLAW_UPDATE_FEED_URL` and `SPARKLE_PUBLIC_ED_KEY` are provided at build time.
- GitHub Releases appcast configuration is now present in local release builds, but the generated `v0.1.1` artifacts were not uploaded.
- GitHub upload was intentionally skipped because Developer ID signing/notarization is not configured yet.

## Next
- Configure Developer ID signing/notarization, then publish `appcast.xml` + versioned DMG to `hellozim22/QRclaw_release`.
