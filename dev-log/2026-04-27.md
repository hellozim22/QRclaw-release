# 2026-04-27

## Wave 1 Follow-up: RLS-09 downgrade

- Issue: d855faa5-cb2e-43aa-9e7b-c7aecdf4e96f
- Decision: RLS-09 remains downgraded because no plugin-scope JWT signer exists in Wave 1.
- Reason: current plugin/agent authentication is Bearer API key based (`sha256(api_key) -> agents.api_key_hash`), not plugin-scope JWT based. A forged `Bearer plugin-agent-token` is rejected by PostgREST as malformed JWT (`PGRST301`) before private rows can be returned.
- Test action: `owner-agent-chat-rls.test.ts` now names the downgraded RLS-09 behavior explicitly, checks anon baseline returns no rows, checks invalid plugin-style bearer is rejected, and adds `RLS-09b.skip` for Wave 3.
