# Add inside nginx.conf `http { ... }` block (once per server).
# Required for WebSocket Upgrade header in proxy locations.

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
